# Policy-as-code guardrails

- Enforce deploy policies via OPA/Rego with unit tests and staged rollout checks.
- Block risky changes (privileged containers, hostPath mounts, cleartext secrets) before they hit the cluster.
- Ships with certified policy packs for PCI, SOC 2, and common gaming security standards.
