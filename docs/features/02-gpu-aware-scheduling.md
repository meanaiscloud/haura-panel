# GPU-aware scheduling

- Detects vendor capabilities (NVIDIA, AMD) and exposes GPU resource classes with MIG and SR-IOV awareness.
- Enforces GPU quota per tenant and per application, with live utilization dashboards.
- Supports mixed CPU/GPU autoscaling policies tied to latency SLOs.
