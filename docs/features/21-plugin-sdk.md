# Plugin and extension SDK

- Stable extension points for panel UI, API middleware, and wings runtime hooks.
- Versioned SDKs with contract tests and sandboxing to prevent extensions from breaking upgrades.
- Marketplace publication flow with automated security scanning and signature verification.
