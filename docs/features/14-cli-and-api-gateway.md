# CLI and API gateway

- Unified GraphQL/REST gateway with JWT/OAuth2 support and per-token scopes.
- First-class CLI (`hauractl`) for lifecycle operations, diagnostics, and drift detection.
- Native SDKs for Go, PHP/Laravel, and TypeScript clients with typed schemas.
